version: "3.8"
services:
  plane-pm-agent:
    build: .
    container_name: plane-pm-agent
    restart: always
    env_file:
      - .env
    ports:
      - "8212:8212"
    networks:
      - plane-pm-net

networks:
  plane-pm-net:
    name: plane-pm-net
    driver: bridge